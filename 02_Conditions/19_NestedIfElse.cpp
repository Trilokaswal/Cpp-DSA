#include <iostream>
using namespace std;
int main () {
    int n;
    cout << "Enter a number: ";
    cin >> n;

if ( n > 0 ) {
              if (n %2 ==0) 
                           { cout << "positive and even number"; }
              else
                   cout << "positive and odd number";      }
else 
    { cout << "It's not a positive number"; }

return 0;

// Formality wala Commit 2
                                                                    }
